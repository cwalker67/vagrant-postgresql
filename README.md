To run vagrant-postgresql, 

    git clone http://github.com/cwalker67/vagrant-postgresql.git
    cd vagrant-postgresql

Get needed submodules

    git submodule init
    git submodule update

Fire up vagrant VM

    vagrant up
